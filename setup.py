#!/usr/bin/env python

from setuptools import setup, find_packages

# import os
# os.system("pip install -r requirements.txt")

setup(name='diveclass',
      version='0.0.2',
      description='diveclass',
      author='libingkun',
      author_email='mail@libingkun.com',
      install_requires=[],
      packages=find_packages(),
      scripts=[],
      package_data={'': ['*.dat']})
